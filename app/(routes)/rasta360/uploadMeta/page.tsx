import MetadataForm from "@/Component/Rasta360/AddMetadata";
import React from "react";

const page = () => {
    return (
        <div className="min-h-screen flex items-center justify-center">
            <MetadataForm />
        </div>
    );
};

export default page;
